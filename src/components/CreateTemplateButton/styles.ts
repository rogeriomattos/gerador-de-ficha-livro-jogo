import styled from "styled-components";

export const Button = styled.button`
  width: 100%;
  display: block;
  background: #649dfd;
  color: #fff;
  border: none;
  cursor: pointer;
  text-transform: uppercase;
`;
